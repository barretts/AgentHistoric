=== RICH-PREAMBLE ===

I’m treating this as a foundational architecture request, so I’m first loading the startup guidance and the architecture skill, then I’ll see whether the feature context is already present in the workspace or whether I need one clarifying detail from you.

---

I’ve confirmed this should be answered in the architecture contract format. I’m loading the remaining startup/router guidance and skimming the workspace for any feature spec so I don’t force you into a clarification if the context is already here.

---

The startup file points at a `gpt` rules subfolder, but those exact files don’t exist at the expected path. I’m locating the actual rule layout now and checking the repo root for any project docs that describe the feature.

---

Selected Subfolder: `gpt`
Selected Expert: `expert-architect-descartes`
Reason: The request is for a `data model` and `trust boundaries`, which is foundational architecture, but the feature itself is not described, so any concrete schema or boundary map would be guesswork.
Confidence (0-1): `0.43`

What is the feature, specifically? A brief spec is enough: actors, main user flow, data created/read/updated, external systems involved, and any auth/privacy constraints.
